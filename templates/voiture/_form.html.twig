{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary"><i class="bi bi-plus-circle"></i> 
    {{ button_label|default('Enregistrer une voiture') }}</button>
{{ form_end(form) }}
