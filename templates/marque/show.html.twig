{% extends 'base.html.twig' %}

{% block title %}Marque{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marque.id }}</td>
            </tr>
            <tr>
                <th>NomMarque</th>
                <td>{{ marque.nomMarque }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('marque_index') }}">Retour</a>

    <a class="btn btn-primary" href="{{ path('marque_edit', {'id': marque.id}) }}">Mofidier</a>

{% endblock %}
