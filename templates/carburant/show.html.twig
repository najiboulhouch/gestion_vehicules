{% extends 'base.html.twig' %}

{% block title %}{% trans %}Carburant{% endtrans %}{% endblock %}

{% block body %}
    <h1>{% trans %}Carburant{% endtrans %}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carburant.id }}</td>
            </tr>
            <tr>
                <th>{% trans %}Type de carburant{% endtrans %}</th>
                <td>{{ carburant.nomCarburant }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('carburant_index') }}">{% trans %}Retour{% endtrans %}</a>

    <a class="btn btn-primary" href="{{ path('carburant_edit', {'id': carburant.id}) }}">{% trans %}Modifier{% endtrans %}</a>

{% endblock %}
