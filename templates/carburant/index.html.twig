{% extends 'base.html.twig' %}

{% block title %}Carburant index{% endblock %}

{% block body %}
    <h1>Carburant index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomCarburant</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for carburant in carburants %}
            <tr>
                <td>{{ carburant.id }}</td>
                <td>{{ carburant.nomCarburant }}</td>
                <td>
                    <a href="{{ path('carburant_show', {'id': carburant.id}) }}">show</a>
                    <a href="{{ path('carburant_edit', {'id': carburant.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('carburant_new') }}">Create new</a>
{% endblock %}
