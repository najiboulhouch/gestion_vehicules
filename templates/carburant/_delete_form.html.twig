<form method="post" action="{{ path('carburant_delete', {'id': carburant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carburant.id) }}">
    <button class="btn">Delete</button>
</form>
